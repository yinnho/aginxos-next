# crond root crontab — busybox crond reads /etc/crontabs/root.
# N5④ (#86 v2, local line): daily snapshot of the state world at 04:17 —
# /home/.aginx + /var/lib/aginx (secret/ excluded, §9) + /etc/aginx into
# /var/backups/aginx/, keep 7. Off-minute on purpose (fleet-uniform :00
# piles up on the server when the push half exists someday). Log at
# /var/log/aginx-backup.log; rcS starts crond with -c /etc/crontabs.
17 4 * * * /usr/bin/aginx-backup now >> /var/log/aginx-backup.log 2>&1
